<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ALL Posts</title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <div class="pp">
      <h1>📢Echo -</h1>
      <h1>“Speak, Share, Echo....”</h1>
    </div>

    <hr class="tt">

    <div class="bbc">
      <a class="bb" href="http://localhost:8080/posts/new">
        <b>Create new Post✏️</b>
      </a>
    </div>

    <diV class="bd">
      <% for(post of posts_arr){ %>
      <div class="ssd">
        <h2 class="ssu">👤<%= post.username%></h2>
        <hr />
        <h4 class="ssc"><%= post.content %></h4>

        <div class="bkc">
          <button class="b1" onclick="location.href='/posts/<%= post._id %>'">
            See in Detail
          </button>
          <button
            class="b2"
            onclick="location.href='/posts/<%= post._id %>/edit'"
          >
            Click to edit
          </button>
          <form method="post" action="/posts/<%=post._id%>?_method=DELETE">
            <button class="b3">Click to Delete</button>
          </form>
        </div>
      </div>
      <%}%>
    </diV>
  </body>
</html>
